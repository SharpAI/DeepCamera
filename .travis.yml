arch:
  - arm64
os: linux
language: c
service: docker
script:
  - ls
  - docker login --username shareai --password $DOCKER_HUB_TOKEN
  - cd docker/build/tensorflow && docker built -f Dockerfile.arm64v8 -t shareai/tensorflow:arm64v8_latest .
