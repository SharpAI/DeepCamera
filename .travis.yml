arch:
  - arm64
os: linux
language: c
service: docker
script:
  - cd docker/build/tensorflow && docker build -f Dockerfile.arm64v8 -t shareai/tensorflow:arm64v8_latest .
after_success:
  - docker login --username shareai --password $DOCKER_HUB_TOKEN
  - docker push shareai/tensorflow:arm64v8_latest
