1. compile yolov2.tar, yolov2, yolov2.params in 7420
   docker run -v `pwd`/test:/export -it solderzzc/rocketchat:tvm_06152018_x86 /bin/bash
   in docker, run github/fast_ai/yolo_parking/convert_tvm_7420.py  

2. need recompile _convert.so, replace github/fast_ai/fast_od/convert/src/convert.c with this convert.c and then compile.

3. in 7420 board
  copy converted yolov2.tar, yolov2, yolov2.params, yolov2.cfg, libdarknet.so to model/yolo directory
  replace new compiled _convert.so
  
  run  redis-server& 
  run  sh work.sh
  run  sh call.sh

